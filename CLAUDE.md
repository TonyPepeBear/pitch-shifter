## 語言與專案定位
- 永遠使用繁體中文（台灣，zh-TW）回覆、撰寫文案與錯誤訊息。

## 開發指令與環境
- 使用 yarn，執行任何 yarn 指令前先切到 Node 22。
- 指令慣例：`nvm use 22 && yarn dev`、`nvm use 22 && yarn typecheck`、`nvm use 22 && yarn build`。
- 不使用 npm/pnpm/bun 取代 yarn。
